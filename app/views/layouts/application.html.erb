<!DOCTYPE html>
  <head>
    <title>Federal Register <%= ":: #{yield(:page_title)}" unless yield(:page_title).empty? %></title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="keywords" content="government federal register executive rulings notices" />


    <!-- Tell search engines to use the proper hostname and protocol -->
    <%= tag(:link, :rel  => 'canonical', :href => 'https://www.federalregister.gov' + request.fullpath.sub(/\/$/,'')) %>


    <link rel="Shortcut Icon" href="/favicon.ico">
    <meta http-equiv="X-UA-Compatible" content="chrome=1">


    <script src="https://use.typekit.net/rgf3yox.js"></script>
    <script>try{Typekit.load();}catch(e){}</script>


    <%# Facebook uses these for parsing when someone adds a link  %>
    <%# TODO: for facebook image below, show curated image if present %>
    <link rel="image_src" href="https://www.federalregister.gov/images/fbdisplay.jpg" />

    <meta name="medium" content="news" />

    <%= tag(:meta, :name => 'title', :content => yield(:page_title) ) if yield(:page_title) %>
    <%= tag(:meta, :name => 'description', :content => yield(:description) ) if yield(:description) %>


    <%= esi layout_head_content_path %>
    <%= stylesheet_link_tag asset_path('application.css') %>

    <!--[if lte IE 9]>
      <%= stylesheet_link_tag asset_path('application-ie.css'), :media => "all" %>
    <![endif]-->
    <!--[if lte IE 8]>
      <%= stylesheet_link_tag asset_path('application-ie8lte.css'), :media => "all" %>
    <![endif]-->
    <!--[if lte IE 7]>
      <%= stylesheet_link_tag asset_path('application-ie7lte.css'), :media => "all" %>
    <![endif]-->

    <%= javascript_include_tag asset_path('application.js') %>

    <%= yield :robots %>
    <%= yield :javascripts %>
    <%= yield :feeds %>

    <%= add_handlebars_template('flickr_exit_modal', 'flickr-exit-modal') %>
    <%= add_handlebars_template('print_disclaimer', 'print-disclaimer') %>
  </head>

  <body id="<%= controller_name %>" class="<%= action_name %>">
    <%= render partial: 'layouts/site_header' %>

    <%= esi site_notification_path(:identifier => :global) %>

    <div id="main">
      <%= yield %>
    </div>

    <%= esi footer_path %>
  </body>
</html>
