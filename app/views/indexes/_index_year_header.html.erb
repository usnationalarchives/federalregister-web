      <div class="row">
        <div class="col-xs-12 col-md-12">
          <div class="page_summary">
            <div class="title-bar clearfix">
              <h1 class="pull-left"><%= year %> Federal Register Index</h1>
              <%= form_tag(select_index_year_path(year),
                :method => :get, :id => "fr-index-year-select", :class => 'pull-right') do %>
                <%= label_tag :year, "View other years:" %>
                <%= select_tag(:year,
                               options_for_select(['2015','2014','2013'], year),
                               :include_blank => false
                              ) %>
                <!-- B.C. TODO: Implement dynamic year selection -->
              <% end %>
            </div>
            <p>
              This index provides descriptive entries and Federal Register page numbers
              for documents published in the daily Federal Register. It includes entries, with select metadata for all documents published in the <%= year %> calendar year.
            </p>
            <p>
              Each agency below is followed by a count that represents the number of documents for that agency in the <%= year %> FR Index. The count is omitted for agency publishing less than 2 documents in a given year.
            </p>
            <p>This online index is current as of <strong><%= "Stubbed: February 13, 2015" %></strong>.
            </p>
            <p>
              <%= link_to root_path do %>
                <span class="icon-fr2 icon-fr2-doc-pdf"></span> Stubbed: Download Approved December 2014 Index (PDF)
              <% end %>
            </p>
            Jump to Agencies: <%= agency_first_letters.map{
              |letter| (link_to(letter.upcase, "#letter-#{letter}"))
            }.join(' | ').html_safe %>
            <!-- B.C. TODO: Confirm whether .html_safe is best here -->
          </div>
        </div>
      </div>
