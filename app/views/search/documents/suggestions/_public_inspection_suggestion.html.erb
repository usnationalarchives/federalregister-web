<%= notification(
  ContentNotification.new(
    text: "There #{public_inspection_suggestion.count == 1 ? 'is' : 'are'} #{pluralize(public_inspection_suggestion.count, "document")} on Public Inspection scheduled for publication that #{public_inspection_suggestion.count == 1 ? 'matches' : 'match' } your search.",
    link: link_to("View Results",
      public_inspection_search_path(
        conditions: public_inspection_suggestion.conditions
      )
    ),
    type: :success,
    icon: 'clipboards',
    options: {
      html: {class: 'ga-exclude'}
    }
  )
) %>
