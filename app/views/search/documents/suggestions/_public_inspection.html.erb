<div class="suggestion">
  <%= fr_icon 'clipboards' %>

  <div class="suggestion-content">
    <p>
      There <%= public_inspection.count == 1 ? 'is' : 'are' %>
      <%= link_to pluralize(public_inspection.count, "document"),
        public_inspection_search_path(
          conditions: public_inspection.conditions
        ) %>
      on Public Inspection scheduled for publication that
      <%= public_inspection.count == 1 ? 'matches' : 'match' %>
      your search.
    </p>
  </div>
</div>
