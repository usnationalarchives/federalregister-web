<li id="document-dates-browse-preview" class="preview">
  <div class="bootstrap-scope">

    <div class="row">
      <div class="<%= bootstrap_col xs: 12, md: 12 %>">
        <div class="fr_stats">
          <h2>
            Current Issue

            <% if doc_presenter.page_count %>
              <span class="supplemental">
                <%= pluralize(doc_presenter.page_count, 'Page') %>
              </span>
            <% end %>
          </h2>

          <div class="<%= bootstrap_col xs: 12, md: 12 %> fr-head-meta-counts">
            <%= render partial: "document_issues/summary_document_counts", locals: {
              navigation: true,
              presenter: doc_presenter
            } %>
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="<%= bootstrap_col xs: 12, md: 12 %>">
        <div class="date_input_holder">
          <h2>Go to a specific date</h2>
          <%= form_tag document_issues_search_path, :method => :get, :id => "date_selector" do %>
            <label>Go to a specific date:</label>
            <%= text_field_tag :date, '', :placeholder => 'MM/DD/YYYY', :class => "browse_search" %>
            <%= submit_tag 'Go' %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="<%= bootstrap_col xs: 12, md: 12 %>">
        <div class="calendar_holder">
          <h2>Explore</h2>
          <div class="calendars-wrapper">
            <div class="calendar-wrapper cal-double">
              <%= document_calendar_for(
                DocumentIssue.current.publication_date - 1.month,
                table_class: 'cal_first',
                year_select: false,
                view_context: self
              ) %>
            </div>
            <div class="calendar-wrapper cal-double">
              <%= document_calendar_for(
                DocumentIssue.current.publication_date,
                table_class: 'cal_last',
                year_select: false,
                view_context: self
              ) %>
            </div>
          </div>
        </div>
      </div>
    </div>

  </div>
</li>
