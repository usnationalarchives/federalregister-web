<li id="public-inspection-browse-preview" class="preview">
  <div class="bootstrap-scope">

    <div class="row">
      <div class="<%= bootstrap_col xs: 12, md: 12 %> description">
        <p>
          The <%= link_to 'Public Inspection page', current_public_inspection_issue_path %>
          on FederalRegister.gov
          offers a preview of documents scheduled to appear in the next day's
          Federal Register issue. The Public Inspection page may also
          include documents scheduled for later issues, at the request
          of the issuing agency.
        </p>
      </div>
    </div>

    <div class="row pi-metadata-bar">
      <div class="<%= bootstrap_col xs: 12, md: 12 %> fr-head-metadata-bar fr-head-metadata-bar-public-inspection">
        <div class="<%= bootstrap_col xs: 12, md: 12 %> fr-head-meta-counts">
          <div class="row">
            <div class="<%= bootstrap_col xs: 12, md: 12 %>">
              <%= render partial: "summary_document_counts", locals: {
                full_width: true,
                presenter: @pi_presenter
              } %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="<%= bootstrap_col xs:12, md:12 %>">
        <div class="date_input_holder">
          <h2>Go to a specific date</h2>
          <%= form_tag document_issues_search_path, :method => :get, :id => "date_selector" do %>
            <label>Go to a specific date:</label>
            <%= text_field_tag :date, '', :placeholder => 'MM/DD/YYYY', :class => "browse_search" %>
            <%= submit_tag 'Go' %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="<%= bootstrap_col xs:12, md:12 %>">
        <div class="calendar_holder">
          <h2>Explore</h2>

          <div class="calendars-wrapper">
            <div class="calendar-wrapper cal-double">
              <%= public_inspection_calendar_esi_for(
                PublicInspectionDocumentIssue.current.publication_date - 1.month,
                table_class: 'cal_first',
                year_select: false
              ) %>
            </div>
            <div class="calendar-wrapper cal-double">
              <%= document_calendar_esi_for(
                PublicInspectionDocumentIssue.current.publication_date,
                table_class: 'cal_last',
                year_select: false
              ) %>
            </div>
          </div>
        </div>
      </div>
    </div>
    
  </div>
</li>
