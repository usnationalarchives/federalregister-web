{{#with folder}}
  <li data-slug="{{slug}}">
    <a href="">
      <span class="name">{{name}}</span>
      <span class="document_count">
        (<span class="document_count_inner">{{doc_count}}</span>)
      </span>
      <span class="loader"></span>
    </a>
  </li>
{{/with}}
